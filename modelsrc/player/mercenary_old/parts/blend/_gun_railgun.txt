_gun_railgun.blend EXPORT notes

FUCK

Renaming:
primary > railgun

Animation conversions:

RELOAD not used

primary_aimmatrix_run             <---  primary_aimmatrix_idle
primary_aimmatrix_swim            <---

primary_swim_fire                 <---  primary_fire

primary_crouch_idle_deployed_aimmatrix <--- primary_aimmatrix_deployed

crouch_walk uses only center and N